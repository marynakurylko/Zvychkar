package com.example.vibehabit.ui.theme

import androidx.compose.ui.graphics.Color

// 1. Глибокі темні фони (Deep Dark)
val BackgroundDark = Color(0xFF0F0F13) // Майже чорний, як нічне небо
val SurfaceDark = Color(0xFF1A1A20)    // Трохи світліший для карток
val SurfaceVariantDark = Color(0xFF26262F) // Для інпутів та кнопок

// 2. Неонові акценти
val NeonPurple = Color(0xFF9D4EDD) // Той самий фірмовий колір зі скріншоту
val NeonBlue = Color(0xFF00E5FF)
val NeonPink = Color(0xFFFF007F)
val NeonGreen = Color(0xFF00FF7F)

// 3. Тексти
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFA0A0AB) // Приглушений сірий для підзаголовків

// Кольори для світлої теми (хоча наш дизайн орієнтований на темну)
val BackgroundLight = Color(0xFFF5F5F7)
val SurfaceLight = Color(0xFFFFFFFF)